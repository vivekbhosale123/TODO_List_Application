package org.todo.repository;
import org.todo.controllerr.register;
import org.todo.module.RegisterModel;
public interface registeraddrepository {
	public boolean isAddRegister(RegisterModel rm);
}
